import pandas as pd
import pyodbc as py
import datetime
import tkinter
from message import get_input
from duplicate_cpn import duplicated_cpn
from missing_cpn import uncleared_cpns
from to_excel import def_init_excel

use_sql = True

begin_date = get_input("Please enter the begin date: ")
recon_date = get_input("Please enter a Recon Date")
print(begin_date)


df_duplicated = duplicated_cpn(use_sql=use_sql,date=begin_date)
df_uncleared = uncleared_cpns(recon_date=recon_date,begin_date=begin_date)[0]


def_init_excel(uncleared_cpns=df_uncleared,duplicated_cpns=df_duplicated,recon_date=recon_date)
